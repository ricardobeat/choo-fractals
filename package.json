{
  "name": "react-fractals",
  "version": "0.0.1",
  "private": true,
  "devDependencies": {
    "preact-scripts": "^0.3.0-alpha",
    "react-scripts": "0.7.0"
  },
  "dependencies": {
    "d3-scale": "^1.0.4",
    "d3-selection": "^1.0.3",
    "preact": "^7.1.0"
  },
  "scripts": {
    "start": "preact-scripts start",
    "build": "preact-scripts build",
    "test": "preact-scripts test --env=jsdom",
    "eject": "preact-scripts eject"
  }
}
